# 05A 代码骨架与文件结构：拿到题以后到底建什么

这份文件解决一个问题：

**我拿到一道“小应用 / API”题以后，不想从空文件夹开始想：到底建什么文件、叫什么、放哪里。**

原则：

- 三小时面试不是企业项目，不要一开始建十几层目录。
- 先用最小结构跑通核心链路。
- 只有当复杂度真的出现时才拆文件。
- 文件结构必须服务于：**容易实现、容易测试、容易部署、容易解释。**

---

# 一、先判断属于哪一类项目

拿到题以后先问：

1. 是纯 API / backend 吗？
2. 是否需要 persistence？
3. 是否调用外部 API？
4. 是否有多个资源类型 / 多组 endpoints？
5. 是否真的需要 frontend？
6. 是否有后台任务 / job 状态？
7. 是否涉及 AI / model API？

根据答案加文件，不要一开始全加。

---

# 二、极简版：题目很简单时

如果只有 2–4 个 endpoint、业务逻辑很少：

```text
project/
├── main.py
├── requirements.txt
├── tests/
│   └── test_main.py
├── README.md
└── .gitignore
```

## main.py

负责：

- 创建 `FastAPI()` app
- 定义少量 endpoint
- Pydantic request/response model
- 简单业务逻辑
- 简单内存状态

适合：

- 简单 CRUD
- 小型转换 API
- 很小的 utility service
- 时间已经非常紧

**如果一个 main.py 只有 150–250 行，而且逻辑清楚，不需要为了“专业”强行拆。**

---

# 三、默认版：我最推荐的三小时 FastAPI 骨架

绝大多数“小应用 / API”题优先从这个开始：

```text
project/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── schemas.py
│   ├── service.py
│   └── store.py
├── tests/
│   └── test_api.py
├── requirements.txt
├── README.md
└── .gitignore
```

这套结构已经足够表达：

```text
HTTP Request
    ↓
main.py
    ↓
schemas.py validation
    ↓
service.py business logic
    ↓
store.py state / persistence
    ↓
HTTP Response
```

---

# 四、每个文件到底干什么

## `app/main.py`

这是应用入口。

只负责：

- 创建 FastAPI app
- health endpoint
- API route
- 调用 service
- 把业务错误转成 HTTP response

不要在这里堆很多业务逻辑。

思路：

```text
request
→ validate
→ call service
→ return response
```

---

## `app/schemas.py`

放 Pydantic models。

例如：

```text
CreateJobRequest
JobResponse
UpdateItemRequest
ErrorResponse
```

这里负责：

- request shape
- response shape
- field validation
- API contract

不要在这里写数据库逻辑。

---

## `app/service.py`

放真正的业务规则。

例如：

```text
create_job()
get_job()
update_job()
calculate_result()
process_request()
```

这里回答：

> “这个应用真正做什么？”

如果 Tech Lead 问业务逻辑，通常这里最重要。

---

## `app/store.py`

默认是最简单的数据存储层。

如果题目不要求真正数据库，可以先：

```text
dict
list
in-memory state
```

例如：

```text
jobs = {}
items = {}
```

把它单独放出来的好处：

- main.py 不乱
- service.py 不跟 HTTP 强绑定
- 后面要换 SQLite 比较容易解释

但是如果项目真的非常小，也可以不拆 `store.py`。

---

## `tests/test_api.py`

优先用：

```text
pytest
FastAPI TestClient
```

主要测：

- happy path
- invalid input
- not found
- duplicate / conflict
- core business rule

不要为了 coverage 写十几个无意义测试。

---

## `requirements.txt`

至少可能包括：

```text
fastapi
uvicorn
pytest
httpx
```

如果用额外数据库 / client，再加对应依赖。

不要留下本地装过但项目根本没用的 package。

---

## `README.md`

最后至少说明：

- What it does
- Core features
- Architecture
- How to run
- How to test
- API examples
- Design decisions
- Known limitations
- Production improvements

---

## `.gitignore`

至少忽略：

```text
__pycache__/
*.pyc
.venv/
.env
.pytest_cache/
```

如果 SQLite 文件不想提交，也加进去。

---

# 五、什么时候加 `database.py`

如果题目明确需要 persistence，或者 in-memory 明显不够：

```text
project/
├── app/
│   ├── main.py
│   ├── schemas.py
│   ├── service.py
│   ├── database.py
│   └── models.py
```

## `database.py`

只处理：

- DB connection
- SQLite initialization
- session / connection helper

## `models.py`

只放数据库层的数据结构。

例如：

```text
Job
User
Item
```

注意：

**Pydantic schema 和 DB model 是两件事。**

但是三小时里，如果使用 Python 标准库 `sqlite3` 而不是 ORM，可以进一步简化：

```text
database.py
```

一个文件就够，不一定需要 `models.py`。

---

# 六、什么时候加 `routers/`

不要一开始就加。

只有当：

- endpoint 比较多
- 有两类以上明显不同的资源
- `main.py` 已经很乱

才变成：

```text
app/
├── main.py
├── schemas.py
├── service.py
├── store.py
└── routers/
    ├── __init__.py
    ├── jobs.py
    └── users.py
```

FastAPI 官方推荐较大应用使用 `APIRouter` 分拆路由。

但是对三小时项目：

**一个资源、一组 endpoint → 不需要 routers 目录。**

例如只有：

```text
POST /jobs
GET /jobs/{id}
GET /jobs
```

全部放 `main.py` 很正常。

---

# 七、如果题目调用外部 API

增加：

```text
app/
├── main.py
├── schemas.py
├── service.py
├── client.py
└── store.py
```

## `client.py`

专门封装：

- HTTP call
- timeout
- external API error
- response parsing

不要把：

```text
requests.get(...)
httpx.get(...)
```

散落在各个 endpoint 里。

Tech Lead 问：

> What if the dependency is down?

就可以围绕 `client.py` 讨论：

- timeout
- retry
- fallback
- circuit breaker
- caching

prototype 不一定实现全部。

---

# 八、如果题目是 Job / Task / Processing API

很可能长这样：

```text
POST /jobs
GET /jobs/{id}
GET /jobs
```

推荐：

```text
app/
├── main.py
├── schemas.py
├── service.py
├── store.py
└── jobs.py        # 只有任务处理逻辑比较复杂时才加
```

状态可以是：

```text
pending
running
completed
failed
```

三小时 prototype：

**优先 synchronous / simple processing。**

除非题目明确要求：

- background workers
- queue
- retry
- async processing

否则不要自己引入 Redis / Celery / Kafka。

可以在 code review 说：

> production 中会拆成 durable queue + worker。

---

# 九、如果题目要求简单 Frontend

不要默认 React / Vue。

如果 UI 只是为了 demo：

```text
project/
├── app/
│   ├── main.py
│   ├── schemas.py
│   └── service.py
├── static/
│   ├── index.html
│   └── app.js
├── tests/
├── requirements.txt
└── README.md
```

FastAPI 提供 backend。

前端：

```text
HTML + CSS + vanilla JS
```

足够时就不要临时搭 React/Vue build chain。

只有题目明确重视 frontend 才另说。

---

# 十、如果题目是 AI / LLM Wrapper

推荐：

```text
app/
├── main.py
├── schemas.py
├── service.py
├── llm_client.py
└── config.py
```

## `llm_client.py`

只负责：

- 调模型
- API key
- timeout
- parse response

## `service.py`

负责：

- prompt / application logic
- result processing
- business rule

不要让 endpoint 里面直接塞一大坨 model call。

如果只有一个环境变量，可以不用 `config.py`，直接简单处理。

---

# 十一、如果题目主要是数据处理

例如：

```text
上传 / 输入一些数据
→ 处理
→ 返回结果
```

可以：

```text
app/
├── main.py
├── schemas.py
└── processor.py
```

`processor.py` 放算法 / 数据处理。

不需要：

```text
service/
repository/
domain/
controller/
```

这种完整企业架构。

---

# 十二、我不建议三小时项目默认创建的东西

除非题目真的需要，否则不要一开始建：

```text
controllers/
repositories/
domain/
entities/
interfaces/
factories/
adapters/
middlewares/
exceptions/
utils/
common/
core/
config/
```

尤其不要为了 Clean Architecture：

```text
router
→ controller
→ service
→ repository
→ adapter
→ database
```

一个很简单的 CRUD 请求穿五六层。

这会造成：

- AI 生成文件很多
- debug 变慢
- import 容易错
- deployment 风险增加
- code review 时自己也不熟

---

# 十三、默认决策树

## 情况 A：简单 API，没有数据库

用：

```text
main.py
schemas.py
service.py
store.py
test_api.py
```

---

## 情况 B：简单 API + SQLite

用：

```text
main.py
schemas.py
service.py
database.py
test_api.py
```

如果数据库模型很多，再加：

```text
models.py
```

---

## 情况 C：API + 外部服务

用：

```text
main.py
schemas.py
service.py
client.py
test_api.py
```

如果还要 persistence：

```text
store.py / database.py
```

---

## 情况 D：多个资源 / endpoint 太多

再加：

```text
routers/
```

---

## 情况 E：简单 UI

再加：

```text
static/index.html
static/app.js
```

---

# 十四、拿到题以后直接给 Claude 的框架 Prompt

> 根据这道题，先不要写业务代码。
>
> 我希望使用 Python + FastAPI。
>
> 这是一个严格 3 小时的 coding interview。
>
> 请根据需求选择“最小足够”的项目结构。
>
> 默认优先考虑：
>
> ```text
> app/
> ├── __init__.py
> ├── main.py
> ├── schemas.py
> ├── service.py
> └── store.py
> tests/
> └── test_api.py
> requirements.txt
> README.md
> .gitignore
> ```
>
> 只有题目真正需要时才增加：
> - database.py
> - models.py
> - client.py
> - routers/
> - static/
>
> 请告诉我：
> 1. 最终建议的目录树
> 2. 每个文件的唯一职责
> 3. 哪些文件现在不要创建
> 4. 第一阶段最少需要哪些文件
>
> 不要为了“专业”增加额外 abstraction。
> 先解释，不要创建文件。

---

# 十五、让 Claude 直接创建骨架

确认结构以后：

> 好，现在只创建刚才确认的最小 project skeleton。
>
> 只做：
> - directories
> - empty/minimal files
> - FastAPI app startup
> - health endpoint
> - requirements
> - basic .gitignore
>
> 不要实现业务逻辑。
>
> 完成后告诉我：
> - 创建了什么
> - 启动命令
> - 如何验证 `/health`
>
> 然后停止，等我下一步。

---

# 十六、我的默认结构一句话记忆

实在来不及想：

```text
main = HTTP
schemas = API 数据格式
service = 业务逻辑
store/database = 数据
client = 外部服务
tests = 验证
README = 解释
```

只要记住这七个角色，绝大多数小型 backend 题都不会完全不知道从哪里开始。

---

# 十七、推荐起步顺序

现场不要一次创建所有东西。

### 第一步

```text
app/main.py
requirements.txt
```

先让：

```text
GET /health
```

能跑。

### 第二步

加：

```text
schemas.py
service.py
```

跑通第一条 core flow。

### 第三步

需要状态才加：

```text
store.py
```

或者：

```text
database.py
```

### 第四步

加：

```text
tests/test_api.py
```

### 第五步

复杂度真的上来，才拆：

```text
routers/
client.py
models.py
```

**不要反过来：先造完整目录，再开始做题。**
