# 01 我的技术背景与默认偏好

这一段可以直接提供给 AI，帮助它做选题和 scope 判断。

> 我的技术背景和偏好：
>
> - 我最熟悉的语言是 **Python**，本次面试优先用 Python 完成后端和核心业务逻辑。
> - 本次面试的默认 backend framework 优先使用 **FastAPI**。原因是它适合快速构建结构清晰的 REST API，request validation、schema 和 API documentation 都比较直接，适合 3 小时 time-boxed prototype。
> - 我有实际 **Flask backend / full-stack development** 经验，做过 Flask + Vue + database 的完整应用，因此我理解 Python Web backend 的基本结构；如果题目或现场环境让 Flask 明显更简单，也可以把 Flask 作为 fallback。
> - 我熟悉 **REST API** 的基本设计和实现，包括：
>   - HTTP methods
>   - JSON request/response
>   - status codes
>   - parameter validation
>   - error handling
> - 我有 **SQL / MySQL / relational database** 经验。
> - 对 3 小时 prototype，如果需求允许，我更倾向 SQLite 或 setup cost 低的 persistence。
> - 如果题目确实要求更完整 relational database，再考虑 PostgreSQL / MySQL。
> - 我有 Vue 3 / HTML / CSS / JavaScript 经验，但这次优先 backend-first。
> - 除非题目明确要求 frontend，否则不要为了 UI 花大量时间。
> - 我熟悉 Git / GitHub。
> - Testing 优先使用 **pytest**。
> - Deployment 相比 Python backend 对我更陌生，因此请降低 deployment complexity，并尽早验证 deployment path。
> - 我喜欢简单、清晰、能够完全解释的 architecture。
> - 不要为了“显得高级”引入 Redis、Kafka、Kubernetes、复杂 microservices、复杂 auth、distributed workers 等不必要组件。
> - 如果题目允许自由选择 Python backend framework，默认优先 **FastAPI**。
> - 只有当 Flask 或其他方案在当前题目 / 环境里明显更简单、更稳、更容易部署时，才建议切换。
> - 不希望为了面试临时使用不熟悉的 Go、复杂 Java stack 或重 frontend architecture，除非题目明确要求。
>
> 在选题和设计时，请优先考虑：
> 1. 3 小时完成率
> 2. correctness
> 3. deployability
> 4. demo 完整度
> 5. 我能否真正理解全部关键代码
> 6. code review 是否有足够 engineering trade-offs 可以讨论
